{
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Backend API",
      "visibility": "public"
    }
  },
  "postCreateCommand": "sudo apt update && sudo apt install -y openjdk-17-jdk && echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64' >> ~/.bashrc && echo 'export PATH=$JAVA_HOME/bin:$PATH' >> ~/.bashrc"
}
